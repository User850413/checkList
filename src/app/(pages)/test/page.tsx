'use client';

export default function Test() {
  // const [myData, setMyData] = useState([]);

  // const { data } = useQuery({
  //   queryKey: ['me'],
  //   queryFn: async () => await getMyDataAgain(),
  // });

  return <>hello</>;
}
